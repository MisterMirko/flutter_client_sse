enum SSERequestType { GET, POST }

/// The default reconnect time. When trying to reconnect it should be used 
/// when no reconnect time is set in a RetryConfiguration
const DEFAULT_RECONNECT_TIME = 5000;
